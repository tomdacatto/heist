1. Always use async/await - All functions must be asynchronous
2. No code comments - Write self-explanatory code without comments  
3. Use context embed methods - Use ctx.approve(), ctx.warn(), ctx.deny(), ctx.neutral() instead of manual embeds
4. Use config colors - Always use ctx.config.colors.* instead of hardcoded colors
5. Minimal code only - Write the absolute minimum code needed to solve the problem